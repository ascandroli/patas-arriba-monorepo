{
  "name": "patas-arriba-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for Fundacion Patas Arriba — volunteer coordination platform",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "npm run dev:server & npm run dev:client",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "engines": {
    "node": ">=24"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ascandroli/patas-arriba-monorepo.git"
  },
  "devDependencies": {
    "@devcontainers/cli": "^0.85.0",
    "@playwright/test": "^1.58.2",
    "bcrypt": "^6.0.0",
    "mongodb": "^7.1.1"
  }
}